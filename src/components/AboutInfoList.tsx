import React from "react"
import { AboutInfoItem } from "./AboutInfoItem"
// import { aboutMe } from "../temp/aboutMe"

export const AboutInfoList: React.FC = () => {

	return (
		<ul className="info__private private">
			{/* {aboutMe.map(info => {
				return <AboutInfoItem info={info} key={info.id} />
			})} */}
		</ul>
	)
}